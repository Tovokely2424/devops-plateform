prasolonjatovo@devopsubuntuhost:~/cobeil/general/configmap-screts$ cat nginx.conf
events {}

http {
    server {
        listen 80;
        location / {
            return 200 'Hello from custom Nginx configuration!';
            add_header Content-Type text/plain;
        }
    }
}
